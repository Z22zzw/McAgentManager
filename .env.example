# Demo mode is used when these values are absent.
# Real mode: credentials are injected into the API process only.
MC_AI_MANAGER_PORT=8787
DSH_BASE_URL=http://127.0.0.1:3080
MCSM_BASE_URL=http://127.0.0.1:23333
MCSM_DAEMON_ID=replace-with-certified-daemon-id
MCSM_TOKEN=inject-at-runtime
MCSM_COOKIE=inject-at-runtime
